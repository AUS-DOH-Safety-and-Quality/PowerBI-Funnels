import { IRNG } from '../rng';
export declare function Exponential(rng?: IRNG): {
    dexp: (x: import("src/lib/types").numVector, rate?: number, asLog?: boolean) => import("src/lib/types").numVector;
    pexp: (q: import("src/lib/types").numVector, rate?: number, lowerTail?: boolean, logP?: boolean) => import("src/lib/types").numVector;
    qexp: (p: import("src/lib/types").numVector, rate?: number, lowerTail?: boolean, logP?: boolean) => import("src/lib/types").numVector;
    rexp: (n: number, rate?: number) => import("src/lib/types").numVector;
};
