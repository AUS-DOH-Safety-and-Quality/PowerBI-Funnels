import { IRNG } from '../rng/irng';
export declare function afc(i: number): number;
export declare function rhyper(N: number, nn1in: number, nn2in: number, kkin: number, rng: IRNG): number | number[];
