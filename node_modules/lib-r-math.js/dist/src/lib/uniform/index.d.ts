import { dunif } from './dunif';
import { punif } from './punif';
import { qunif } from './qunif';
import { IRNG } from '../rng';
export declare function Uniform(rng?: IRNG): {
    dunif: typeof dunif;
    punif: typeof punif;
    qunif: typeof qunif;
    rng: IRNG;
    runif: (n?: number, min?: number, max?: number) => import("src/lib/types").numVector;
};
