import { IRNG } from '../';
export declare abstract class IRNGNormal {
    protected rng: IRNG;
    constructor(_rng: IRNG);
    norm_rand(): number;
    norm_rand(n: 0 | 1): number;
    norm_rand(n: number): number | number[];
    protected abstract internal_norm_rand(): number;
    unif_rand(): number;
    unif_rand(n: 0 | 1): number;
    unif_rand(n: number): number | number[];
}
